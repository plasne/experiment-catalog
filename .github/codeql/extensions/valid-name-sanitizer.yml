extensions:
  # Mark ValidNameAttribute.IsValid as a barrier guard for log injection
  # The attribute validates input contains only safe characters:
  # letters, digits, hyphens, underscores, periods, and colons
  # When IsValid returns true, the input is guaranteed safe
  - addsTo:
      pack: codeql/csharp-all
      extensible: barrierGuardModel
    data:
      # Parameters: namespace, type, subtypes, name, signature, ext, input, acceptingvalue, kind, provenance
      - [
          "Catalog",
          "ValidNameAttribute",
          true,
          "IsValid",
          "(System.Object)",
          "",
          "Argument[0]",
          "true",
          "log-injection",
          "manual",
        ]
